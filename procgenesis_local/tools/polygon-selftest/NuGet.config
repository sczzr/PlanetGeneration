<?xml version="1.0" encoding="utf-8"?>
<configuration>
  <!--
    自检工程不需要任何外部包（只引用地块核心源码），
    因此清空包源与回退目录，避免依赖用户级 NuGet 配置。
  -->
  <packageSources>
    <clear />
  </packageSources>
  <fallbackPackageFolders>
    <clear />
  </fallbackPackageFolders>
</configuration>
